//add n numbers
#include<stdio.h>
void main()
{
	int n,i,addition;
	printf("enter how many numbers");
	scanf("%d",&n);
	addition=num[1];
	for(i=0;i+n;i++);
	{
		if(sum=i+n; i++);
		{
			addition=num [1]
		}
		printf("addition is %d",addition)
	}
}
